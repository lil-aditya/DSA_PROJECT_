
#pragma once //gives instructions to the compiler or preprocessor to change how code is compiled without changing the program's logic
#include <string>
#include <cstdint>
uint64_t simpleHash(const std::string &data);
